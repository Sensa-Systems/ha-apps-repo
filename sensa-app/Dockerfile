ARG BUILD_FROM
FROM ${BUILD_FROM}

# This Dockerfile exists as a fallback for local development.
# In production, the Supervisor pulls the pre-built image
# specified by the "image" field in config.yaml.
